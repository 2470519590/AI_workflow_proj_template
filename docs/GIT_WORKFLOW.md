After completing a large task, write a dated task record under `tasks/`.

If a task fixes a concrete problem, update `docs/ERROR_CATALOG.md`.

If a task confirms or changes an engineering decision, update `docs/DECISIONS.md`.

If a task affects control algorithms, observers, estimators, state vectors, or command semantics, update `docs/CONTROL_THEORY.md`.
